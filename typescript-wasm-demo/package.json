{
  "name": "typescript-wasm-demo",
  "version": "0.1.0",
  "description": "TypeScript KV Store using Rust Calimero SDK bindings",
  "type": "module",
  "main": "dist/kv-store.js",
  "types": "dist/kv-store.d.ts",
  "scripts": {
    "build": "tsc",
    "build:wasm": "cargo build --target wasm32-unknown-unknown",
    "dev": "tsc --watch",
    "clean": "rm -rf dist target",
    "test": "echo 'Tests will be added later'"
  },
  "keywords": [
    "calimero",
    "wasm",
    "typescript",
    "rust",
    "kv-store",
    "blockchain"
  ],
  "author": "Calimero Network",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
