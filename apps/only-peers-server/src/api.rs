pub struct Post {
    content: String,
}

pub fn create_post(post: Post) {
    //check if user has permissions to create a post

    //create post
}

pub fn comment_post(post: Post) {
    //check signature
}

pub fn fetch_posts(post: Post) -> Vec<Post> {
    //check signature

    vec![]
}
