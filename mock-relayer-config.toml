# Mock Relayer Configuration
# This configuration enables the mock-relayer protocol for local development

listen = "0.0.0.0:9812"

[protocols.mock-relayer]
enabled = true
network = "local"
rpc_url = "http://localhost:9812"
contract_id = "mock-context-config"
