name: Profiling Container release
description: "Release profiling container with binaries built with frame pointers enabled"

inputs:
  version:
    description: "Version of the release"
    required: true
  release:
    description: "Flag indicating if this is a release"
    required: true
  prerelease:
    description: "Flag indicating if this is a prerelease"
    required: true
  push:
    description: "Flag indicating if the tag(s) should be pushed to the registry"
    required: true
  binaries:
    description: "List of binaries to include in the container"
    required: true
  dockerfile:
    description: "Path to the Dockerfile to use for building the container"
    required: true
  container_name:
    description: "Name of the container to build and push"
    required: true
  github-token:
    description: "GitHub token"
    required: true

runs:
  using: "composite"
  steps:
    - name: Download built profiling binaries
      uses: actions/download-artifact@v4
      with:
        pattern: artifacts-profiling-*
        path: binaries-profiling/
        merge-multiple: true

    - name: Extract Linux profiling binaries
      shell: bash
      run: |
        mkdir -p ./bin-profiling/amd64 ./bin-profiling/arm64

        readarray -t binaries <<< "${{ inputs.binaries }}"

        for binary in ${binaries[@]}; do
          echo "Extracting ${binary} profiling binaries"

          tar -xzf binaries-profiling/${binary}_x86_64-unknown-linux-gnu.tar.gz -C ./bin-profiling/amd64 --no-same-owner
          tar -xzf binaries-profiling/${binary}_aarch64-unknown-linux-gnu.tar.gz -C ./bin-profiling/arm64 --no-same-owner
        done

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ inputs.github-token }}

    - name: Prepare Docker Cache parameters
      shell: bash
      id: prepare
      run: |
        MASTER_SCOPE="${{ github.repository_owner }}-docker-master-profiling"
        DEV_SCOPE="${{ github.repository_owner }}-docker-dev-profiling"

        if [ "${{ github.ref }}" == "refs/heads/master" ]; then
          echo "cache_from=type=gha,scope=${MASTER_SCOPE}" >> $GITHUB_OUTPUT
          echo "cache_to=type=gha,scope=${MASTER_SCOPE},mode=max" >> $GITHUB_OUTPUT
        else
          echo "cache_from=type=gha,scope=${MASTER_SCOPE} type=gha,scope=${DEV_SCOPE}" >> $GITHUB_OUTPUT
          echo "cache_to=type=gha,scope=${DEV_SCOPE},mode=max" >> $GITHUB_OUTPUT
        fi

        if [ "${{ github.event_name }}" == "pull_request" ]; then
          echo "docker_source=${{ github.event.pull_request.html_url }}" >> $GITHUB_OUTPUT
        else
          echo "docker_source=https://github.com/${{ github.repository }}/tree/${{ github.sha }}" >> $GITHUB_OUTPUT
        fi

    - name: Extract metadata
      id: metadata
      uses: docker/metadata-action@v5
      env:
        LABELS: |
          org.opencontainers.image.description=Calimero Node with Profiling Tools
          org.opencontainers.image.source=${{ steps.prepare.outputs.docker_source }}
          org.opencontainers.image.licenses=MIT OR Apache-2.0
          org.opencontainers.image.authors=Calimero Limited <info@calimero.network>
          org.opencontainers.image.url=https://calimero.network
        DOCKER_METADATA_ANNOTATIONS_LEVELS: manifest,index
        DOCKER_METADATA_PR_HEAD_SHA: true
      with:
        images: ghcr.io/${{ github.repository_owner }}/${{ inputs.container_name }}
        # Use -profiling suffix for all tags to distinguish from regular images
        tags: |
          type=raw,value=edge-profiling,enable=${{ github.ref == 'refs/heads/master' }}
          type=ref,prefix=pr-,suffix=-profiling,event=pr
          type=sha,prefix=,suffix=-profiling,format=short
          type=raw,value=${{ inputs.version }}-profiling,enable=${{ github.ref == 'refs/heads/master' && inputs.release == 'true' }}
        labels: ${{ env.LABELS }}
        annotations: ${{ env.LABELS }}

    - name: Build and push image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ${{ inputs.dockerfile }}
        push: ${{ inputs.push == 'true' }}
        platforms: linux/amd64,linux/arm64
        tags: ${{ steps.metadata.outputs.tags }}
        labels: ${{ steps.metadata.outputs.labels }}
        annotations: ${{ steps.metadata.outputs.annotations }}
        cache-from: ${{ steps.prepare.outputs.cache_from }}
        cache-to: ${{ steps.prepare.outputs.cache_to }}
        provenance: false
        sbom: false
