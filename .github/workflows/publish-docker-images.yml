name: Build and Publish Docker Images

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - 'feat__*'
    paths:
      - Cargo.toml
      - Cargo.lock
      - 'crates/**'
      - 'core/Dockerfile'
      - '.github/workflows/publish-docker-images.yml'
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - master
    paths:
      - Cargo.toml
      - Cargo.lock
      - 'crates/**'
      - 'core/Dockerfile'
      - '.github/workflows/publish-docker-images.yml'
  workflow_run:
    workflows: ["Release"]
    types:
      - completed

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare Docker Build
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'workflow_dispatch' || 
      github.event_name == 'push' || 
      github.event_name == 'pull_request' || 
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    outputs:
      version: ${{ steps.version_info.outputs.version }}
      should_build: ${{ steps.version_info.outputs.should_build }}
      prerelease: ${{ steps.version_info.outputs.prerelease }}
      merod_tags: ${{ steps.merod_metadata.outputs.tags }}
      merod_labels: ${{ steps.merod_metadata.outputs.labels }}
      meroctl_tags: ${{ steps.meroctl_metadata.outputs.tags }}
      meroctl_labels: ${{ steps.meroctl_metadata.outputs.labels }}
      cache_from: ${{ steps.prep_cache.outputs.cache_from }}
      cache_to: ${{ steps.prep_cache.outputs.cache_to }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version info
        id: version_info
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # TESTING: Always build for testing purposes
          echo "should_build=true" >> $GITHUB_OUTPUT
          echo "prerelease=true" >> $GITHUB_OUTPUT
          
          if [ "${{ github.event_name }}" == "workflow_run" ]; then
            # Extract version from the triggering workflow (Release)
            latest_release=$(gh api repos/${{ github.repository }}/releases/latest --jq .tag_name)
            if [ -n "$latest_release" ]; then
              echo "Using latest release version: $latest_release"
              echo "version=$latest_release" >> $GITHUB_OUTPUT
            else
              echo "No release version found, using dev version"
              echo "version=dev-workflow-run" >> $GITHUB_OUTPUT
            fi
          else
            if [ -f "Cargo.toml" ]; then
              version_candidate=$(cargo metadata --format-version 1 --no-deps | jq -r '.packages[] | select(.name == "merod") | .version')
              echo "Version from Cargo: $version_candidate"
              
              if [ "${{ github.ref }}" == "refs/heads/master" ]; then
                echo "On master branch"
                echo "version=$version_candidate" >> $GITHUB_OUTPUT
              elif [ "${{ github.event_name }}" == "pull_request" ] && [[ "${{ github.head_ref }}" == release/* ]]; then
                echo "On release PR"
                echo "version=prerelease-${{ github.event.number }}" >> $GITHUB_OUTPUT
              elif [[ "${{ github.ref }}" == refs/heads/feat__* ]]; then
                echo "On feature branch"
                branch_name=$(echo "${{ github.ref }}" | sed 's|refs/heads/||')
                echo "version=dev-${branch_name}" >> $GITHUB_OUTPUT
              elif [ "${{ github.event_name }}" == "pull_request" ]; then
                echo "On PR"
                echo "version=pr-${{ github.event.number }}" >> $GITHUB_OUTPUT
              elif [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
                echo "Manual trigger"
                echo "version=$version_candidate-manual" >> $GITHUB_OUTPUT
              else
                echo "Other branch/event"
                echo "version=dev-$(echo ${{ github.ref }} | sed 's|refs/heads/||')" >> $GITHUB_OUTPUT
              fi
            else
              echo "No Cargo.toml found"
              echo "version=dev-unknown" >> $GITHUB_OUTPUT
            fi
          fi
      
      - name: Extract metadata for merod
        id: merod_metadata
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/merod
          tags: |
            type=raw,value=${{ steps.version_info.outputs.version }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/master' }}

      - name: Extract metadata for meroctl
        id: meroctl_metadata
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/meroctl
          tags: |
            type=raw,value=${{ steps.version_info.outputs.version }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/master' }}

      - name: Prepare cache parameters
        id: prep_cache
        run: |
          # Define cache configuration for Buildx
          if [ "${{ github.ref }}" == "refs/heads/master" ]; then
            # Use a master-specific cache for stable releases
            SCOPE="${{ github.repository_owner }}-docker-master"
          else
            # Use a feature branch cache for development
            SCOPE="${{ github.repository_owner }}-docker-dev"
          fi
          
          echo "Using cache scope: $SCOPE"
          echo "cache_from=type=gha,scope=$SCOPE type=registry,ref=ghcr.io/${{ github.repository_owner }}/builder-cache:$SCOPE" >> $GITHUB_OUTPUT
          echo "cache_to=type=gha,scope=$SCOPE,mode=max" >> $GITHUB_OUTPUT

  build-and-push:
    name: Build and Push Docker Images
    needs: prepare
    if: needs.prepare.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        image: [merod, meroctl]
      fail-fast: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          # Enable BuildKit driver with more caching capabilities
          driver-opts: |
            image=moby/buildkit:latest
            network=host
      
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      # Add a step to create a layer cache for Rust dependencies
      - name: Cargo layer caching
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build and push ${{ matrix.image }} image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          target: ${{ matrix.image }}
          tags: ${{ matrix.image == 'merod' && needs.prepare.outputs.merod_tags || needs.prepare.outputs.meroctl_tags }}
          labels: ${{ matrix.image == 'merod' && needs.prepare.outputs.merod_labels || needs.prepare.outputs.meroctl_labels }}
          cache-from: ${{ needs.prepare.outputs.cache_from }}
          cache-to: ${{ needs.prepare.outputs.cache_to }}
          # Use better caching for Rust builds
          build-args: |
            BUILDKIT_INLINE_CACHE=1
            CARGO_NET_GIT_FETCH_WITH_CLI=true
          outputs: type=registry,name=ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}
          provenance: mode=max
          sbom: true
          annotations: |
            org.opencontainers.image.description=${{ matrix.image == 'merod' && 'Merod container image' || 'Meroctl container image' }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.licenses=MIT 